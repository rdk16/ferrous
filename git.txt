git remote add origin https://github.com/rdk16/ferrous.git
git branch -M main
git push -u origin main

# 1. Prepara todos os arquivos modificados
git add .

# 2. Cria um ponto de salvamento com uma mensagem explicando o que mudou
git commit -m "Explique aqui o que você atualizou"

# 3. Envia as atualizações para o servidor remoto
git push origin nome-da-sua-branch

git rm -r --cached node_modules